# CodeHarness Docker 构建上下文排除（PLAN Task 21）
# 镜像只需 package.json + package-lock.json + dist/，其余一律不进上下文。

# 依赖与构建产物（dist/ 必须 COPY 进镜像，不排除）
node_modules
build

# 测试与源码（构建在宿主机完成，镜像不携带）
tests
src
scripts

# 文档与规划（非镜像内容）
docs
.github
AGENT_LOG.md
PLAN.md
SPEC.md
SPEC_PROCESS.md
KNOWN_ISSUES.md
DESIGN_BRIEF.md
TESTING.md

# 版本控制
.git
.gitignore

# 凭据与密钥（绝不进上下文）
.env
.env.*
secrets
credentials
*.cred
*.pem
*.key
*.p12
*.pfx

# 其他 worktree（.claude/worktrees/ 内含完整仓库副本，体积大且含历史敏感信息）、桌面应用、日志
.claude/
desktop/
*.log
logs
