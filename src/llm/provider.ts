import type { LLMProvider, LLMResponse } from '../types.js';

// LLMProvider and LLMResponse are the canonical interfaces defined in src/types.ts.
// This module imports them for use within the llm/ layer.
// No re-export — src/types.ts remains the single source of truth.
